// tailwind.config.js
module.exports = {
    theme: {
        extend: {
            // No specific extension needed for scroll-behavior, it's a global style.
        }
    },
    plugins: [
        // You can use plugins here if necessary.
    ],
}
